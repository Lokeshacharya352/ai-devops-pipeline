apiVersion: v1
kind: Service
metadata:
  name: ai-devops-service
spec:
  type: NodePort
  selector:
    app: ai-devops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007
